import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerStyle: {
    flex: 1,
  },
  buttonStyle: {
    height: 48,
    marginLeft: 0,
    marginRight: 0,
  },
});

export default styles;
