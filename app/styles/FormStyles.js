import { create } from 'react-native-platform-stylesheet';

const styles = create({
  textInputStyle: {
    width: 200,
    height: 36,
    padding: 5,
  },
  selectStyle: {
    width: 150,
  },
});

export default styles;
