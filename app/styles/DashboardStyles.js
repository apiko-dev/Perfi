import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  rootStyle: {
    flex: 1,
  },
  addButtonStyle: {
    position: 'absolute',
    right: 0,
    bottom: 15,
  },
});

export default styles;
